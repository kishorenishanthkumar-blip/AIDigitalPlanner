{
  "name": "aidp-platform-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for aidp-platform.pages.dev · runs after every Pages deploy",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
